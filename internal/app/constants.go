package app

// Application name
const NAME = "Course Sync"

// Application version
const VERSION = "2026.0"

// Executable name
const EXECUTABLE_NAME = "csync"

// Project root marker directory name; folders containing this directory are considered project
// root directory (or simply project directory)
const PROJECT_ROOT_MARKER_DIR_NAME = ".git"

// Configuration file name
const CONFIG_FILE_NAME = "course_sync.config.json"

// Application hidden directory name
const HIDDEN_DIR_NAME = ".course_sync"
